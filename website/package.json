{
  "name": "soar-analytics",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "prepare:deploy-data": "node scripts/prepare-deploy-data.mjs",
    "build": "npm run prepare:deploy-data && next build",
    "start": "next start",
    "check:data": "node scripts/check-published-data.mjs",
    "check:product": "node scripts/check-product-contract.mjs",
    "test": "npm run check:data && npm run check:product",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@neondatabase/serverless": "^1.1.0",
    "next": "^16.3.0",
    "react": "^19.2.0",
    "react-dom": "^19.2.0"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "@types/react": "^19.0.0",
    "@types/react-dom": "^19.0.0",
    "typescript": "^5.9.0"
  }
}

import type {GamePrediction} from "../../lib/michigan/predictions";
import {describeTeamMargin} from "../../lib/michigan/predictions";
export function PredictionPanel({prediction}:{prediction:GamePrediction|null}){if(!prediction)return <section className="empty-state"><span>SOAR MODEL PROJECTION</span><h2>Coming game week.</h2></section>;return <section className="prediction-panel"><span className="eyebrow">SOAR MODEL PROJECTION · PROJECTED</span><h2>{describeTeamMargin(prediction)}</h2><div className="pending-grid prediction-facts"><div><span>PROJECTED WINNER</span><strong>{prediction.predictedWinner.toUpperCase()}</strong></div><div><span>PROJECTED HOME MARGIN</span><strong>{prediction.predictedHomeMargin>0?"+":""}{prediction.predictedHomeMargin.toFixed(1)}</strong></div></div></section>}
